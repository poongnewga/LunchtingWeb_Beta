<h1 class="dk_sr-only">런치팅 - 비밀번호 아이디 찾기</h1>
<h2 class="dk_sr-only">비밀번호 아이디 찾기 페이지 입니다.</h2>
<div class="ks_find">
  <div class="ks_find2">
<h4 id="ks_nick"> 계정정보 찾기 </h1>
<br></br>
<div class="ks_tab2">
  <button class="ks_tablinks2" onclick="openCity(event, 'ks_id')" >ID 찾기</button>
  <button class="ks_tablinks2" onclick="openCity(event, 'ks_pw')" id="ks_defaultOpen" >비밀번호 찾기</button>
  </div>
  <div id="ks_id" class="ks_tabcontent2">
<p class="ks_findtext">가입시 입력한 이메일을 적어주시면 닉네임을 메일로 보내드립니다.</p>
<br>
<div class="ky_error-comment">
  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %> ky_alert">
      <%= message %>
    </div>
  <% end %>
</div>
<%= form_tag find_find_id_path do %>
  Email : <input class="ks_email" type="email" name="email"><br></br>
  <input class="ks_submit" type="submit" value="완료">
<% end %>
</div>

<div id="ks_pw" class="ks_tabcontent2">
  <p class="ks_findtext">가입시 입력한 이메일을 적어주시면 비밀번호를 메일로 보내드립니다.</p>
  <br>
  <div class="ky_error-comment">
    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %> ky_alert">
        <%= message %>
      </div>
    <% end %>
  </div>
<%= form_tag find_find_password_path do %>
  Email : <input class="ks_email" type="email" name="email"><br></br>
  <input class="ks_submit" type="submit" value="완료">
<% end %>
</div>
</div>
</div>

<% content_for :page_js do %>
  <script type="text/javascript">
     <%= render file: "#{Rails.root}/app/assets/javascripts/find.js" %>
  </script>
<% end %>
